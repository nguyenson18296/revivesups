'use strict';

/**
 * settings-homepage service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::settings-homepage.settings-homepage');
